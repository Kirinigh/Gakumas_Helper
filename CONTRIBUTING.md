# 贡献说明

感谢参与。请先阅读[问题反馈与分流](SUPPORT.md)、[与上游的区别](guides/UPSTREAM_DIFFERENCES.md)和[隐私与安全](guides/PRIVACY_AND_SAFETY.md)。

## 先开 Issue

公开 `main` 保留连续的源码历史，可以按实际功能、修复和文档变化多次提交，不要求每次提交都升级版本或发布下载包。`main` 可以领先最近的发布版本；需要安装的用户请使用 Release 下载包。已发布的标签与下载文件不会被覆盖。开始较大修改前请先开 Issue 讨论范围；维护者会把确认采用的修改同步到开发源码，再以经过核验的公开源码提交更新 `main`。

小型、可复现且不涉及私人样本的修复可以直接提交 Pull Request (PR)，但仍须解释根因、行为变化和验证范围。

## 提交说明

每个提交对应一项真实、可说明的逻辑变化，标题直接写清修改内容，必要时用正文解释根因和验证结果。例如：

- `fix(arena): 结果页就绪后再计入识别次数`
- `docs(support): 补充反馈所需的日志信息`

普通功能、修复或文档提交使用相应的 `feat`、`fix`、`docs` 等摘要，不写成发布版本的公告。多个独立变化可以分成多个提交，同一版本号可以保留在这些普通提交中。只有发布新版本（包括预览版）时才提高版本号、整理发布说明、构建下载包并创建标签和 Release；该次发布的标签、源码提交和下载包来源必须一致。

## 变更要求

- 修复根因并保留失败关闭；不得用降低阈值、无限重试、猜测身份或跳过字段验证制造表面通过。
- 真实挑战链优先禁止胜率虚高；不得把综合力或不完整队伍替代为模拟输入。
- 只改问题所需范围，不混入无关格式化、重构或依赖升级。
- Python 改动运行相关测试与 Ruff；JSON/YAML 改动至少完成解析和相关测试。
- 新增外部代码、模型、数据或图库时，必须说明来源 revision、生成方式、许可证/再分发状态和兼容版本。
- 不引入来源不明二进制，不要求关闭 Defender/UAC，不使用游戏注入或内存读取。

## 禁止提交的内容

- 账号、邮箱、Cookie、令牌、密钥、凭据、本机路径或设备标识；
- 完整配置、日志包、缓存、对局记录、截图、ROI、崩溃转储；
- 原始用户游戏截图、私人训练数据或其可链接指纹；
- 未经许可的第三方资源、游戏素材搬运或来源不清的模型；
- 内部任务卡、会话记录、开发仓库历史或未清理的临时文件。

如果识别修复需要私人图像，请先开不含图像的 Issue。未建立合适的非公开传输和清理流程前，不要把样本提交到公开仓库。

## PR 说明

PR 至少写明：

1. 可复现的根因；
2. 为什么现有安全门不足；
3. 修改后的唯一生产路线；
4. 运行过的验证及结果；
5. 未验证环境和回滚方式；
6. 来源/许可证变化（没有则明确写“无”）。

公共 PR 不应修改发布版本、更新仓库、许可证或 Release 资产，除非对应 Issue 已明确批准该发布范围。
