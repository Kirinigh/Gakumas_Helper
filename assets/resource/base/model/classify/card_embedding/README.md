# 卡牌 ID embedding 生产资产

此目录是 TASK-100 使用的单一生产识别资产：64×64 RGB 卡图输入、128 维特征嵌入、Top-5 余弦近邻检索。具体业务 ID 在检索后独立解析强化状态；愚人节视觉复用组还需要可靠的计划类型。

运行时必须校验 `manifest.json` 中的模型、图库和类别表 SHA-256。卡牌身份接受阈值与视觉身份分差在 TASK-100 冻结 DMM 集完成前保持 `null`，因此只读动作会输出候选但安全返回 `UNKNOWN`，不会点击。
