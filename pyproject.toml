[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "MaaGakumasu"
version = "1.4.9+gkh.260824"
description = "学園アイドルマスター(学マス)小助手 | Assistant For Gakuen Idolmaster"
readme = "README.md"
license = {text = "AGPL-3.0-only"}
requires-python = ">=3.12"
authors = [
    {name = "SuperWaterGod"}
]
keywords = ["maa", "maaframework", "gakumas", "automation", "image-recognition"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Win32 (MS Windows)",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: GNU Affero General Public License v3",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Games",
]

dependencies = [
    "maafw",
    "loguru",
    "Pillow",
    "numpy",
    "opencv-python-headless",
    "onnxruntime",
    "PyYAML",
]
[project.urls]
Homepage = "https://github.com/Kirinigh/Gakumas_Helper"
Repository = "https://github.com/Kirinigh/Gakumas_Helper"
Issues = "https://github.com/Kirinigh/Gakumas_Helper/issues"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "ruff>=0.1.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["agent*"]

[tool.ruff]
select = ["I"]
line-length = 144 
target-version = "py312"

[tool.ruff.lint.isort]
length-sort = true
length-sort-straight = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "agent"]
