# 竞技场胜率功能

## 三个用户入口

| 入口 | 是否挑战 | 适用场景 |
| --- | --- | --- |
| 重算竞技场己方总分 | 否 | 首次使用、期数变化、己方编成变化、引擎或样本设置变化 |
| 竞技场胜率分析并挑战（单次） | 条件满足时消耗 1 次 | 单独验证一次完整读取、模拟、选敌和结果记录 |
| 每日挑战 → 胜率选敌 | 条件满足时按日常流程挑战 | 己方缓存已经确认后的日常运行 |

两个独立入口默认都不勾选，也不会自动加入一键预设。请在“一键日常”的任务列表使用“添加任务”加入，再单独勾选运行。

上游原有的自动、最大值、最小值、随机和固定第 1～3 位模式仍然保留。只有“胜率选敌”使用本页所述的完整读取和统计门槛；其他模式继续使用上游自身逻辑。

## 一次胜率任务如何工作

1. 解析所选竞技场期数及其三个固定舞台 ID。
2. 读取并严格校验己方缓存。缓存兼容时只复用已保存的己方编成、分数样本和 Grade，不重新进入 `リハーサル`，也不单独 OCR Grade；只有手动重算或明确启用“自动重算己方数据”才完整重读己方。若仅模拟引擎 revision 变化，则使用缓存编成重新模拟，不重读游戏。
3. 按当前显示顺序读取三名对手，每名对手均读取三个舞台的所有可用成员。
4. 对每名成员读取表现力、支援加成、四个 P 道具槽、两排技能卡、重复卡和自定义强化。每个舞台的三个物理成员槽都会检查；只有已证明为空或不可用的槽不进入模拟，Grade 上限外出现占用或状态不明确时会停止。标为 `重複` 的卡会被识别后排除，也不会点击详情。
5. 使用包内固定版本的 `gakumas-tools` 引擎为每名成员生成分数经验分布。运行时不访问网页，也不要求系统安装 Node.js。
6. 对每次抽样，在同一舞台内分别加总双方 1～3 名成员的原始分；双方所有成员中原始分第一名获得 `20%` 奖励，再比较该舞台双方总分。若双方最高原始分并列且奖励归属会改变结果，该样本保守记为非胜平局。
7. 三个舞台至少赢两场才算整场胜利。对每名对手计算点估计、双侧 95% Wilson 置信区间 (Wilson confidence interval) 以及用于达标的单侧下限。
8. 为抑制从三名对手中选择最大值造成的赢家诅咒 (Winner's curse)，达标下限使用三对手 Bonferroni 家族错误率校正。程序从上到下选择第一名校正下限达到门槛的对手；若三名均未达标，则选择预测胜率最高者，预测胜率并列时选择靠前者。此时仍记录为未达标，不降低门槛或改写达标状态。
9. 点击前再次确认同一 Maa 会话、同一竞技场日、三个有序对手和目标位置。确认后才创建待决记录并点击开始。
10. 挑战后记录三个舞台的赢家与总体胜负，核对三局两胜关系。成功保存后才离开结果页，确认回到竞技场后继续剩余场次；结算分数或奖励演算异常不阻断流程。

已支持的临时故障由客户端在固定次数和时限内恢复，成功后继续当前任务；恢复后仍无法确认真实输入或结果时停止。下述费用二态使用明确限定的保守兜底，其他缺失输入不会用综合力或历史对手补齐。

## 70% 门槛意味着什么

默认达标条件是三对手校正后的单侧 95% Wilson 下限至少 70%，因此被标为达标所需的点估计高于 70%：

- 2000 次模拟时约需 `72.20%` 点估计；
- 4000 次模拟时约需 `71.55%` 点估计。

达标判断为抽样误差留出余量，因此显示的估计胜率略高于 70% 时，也可能尚未达标。无人达标时仍按预测胜率最高者挑战。

默认模拟 2000 次，至少 1000 次。次数越多通常耗时越长、抽样误差越小，估计越稳定；减少次数通常更快，但需要更高的估计胜率才能通过同一置信下限门槛。次数不会改变真实胜率，也不会修复错误编成或错误期数。

## 己方缓存契约

成功重算后，权威缓存和可读摘要位于当前安装目录的 `.local/arena-win-rate`。摘要包含期数、舞台 ID、样本数、seed、引擎版本、支援加成、完整性计数、三舞台分布、有效 Grade 和九名成员概要，不包含截图。每名成员还会显示其原始模拟分数样本的算术平均值，按四舍五入取整数；没有有效缓存的栏位显示“未识别”。这些摘要项是只读说明，不是九个可编辑输入框。

每日挑战的胜率选敌模式提供一个单独的“竞技场等级（Grade）”下拉框，顺序为“未识别、7、6、5、4、3、2、1”。MFA 不会根据缓存动态改写下拉框默认项，因此默认保持“未识别”：它表示清除人工覆盖并复用己方缓存里已识别的 Grade，不表示把有效 Grade 改为空；摘要会显示最终采用的数值和来源。选择 1～7 后，程序会在运行胜率任务时把人工覆盖写回缓存，并继续与实际成员槽结构交叉检查。缓存本身没有已识别 Grade 且仍选择“未识别”时，流程会安全停止。

缓存复用会检查：

- 竞技场期数与三个舞台 ID；
- 固定引擎 revision；
- seed；
- 请求模拟次数是否不大于缓存样本数。

缓存无法知道用户是否刚刚修改了己方编成。期数或编成变化后，用户必须手动重算。若把模拟次数从 2000 提高到 4000，也必须用 4000 次重新生成缓存；请求较小样本时可以从更大的缓存切片复用。

缓存通过上述契约时，日常胜率任务只读取三个对手，不会为了再次识别 Grade 或刷新九名成员平均值而完整重读己方；挑战前后的运行证据会保持 `full_lineup_reread=false`。只有用户显式运行己方重算，或临时开启“自动重算己方数据”，才重新进入己方读取。仅模拟引擎 revision 变化时，旧分数样本会失效，但程序直接使用缓存编成重新模拟，不重新读取游戏。

重算完成后，Maa 设置页不会自动监听摘要文件变化。切换到其他任务再返回，或关闭后重新打开设置页，即可刷新说明。

## 支援加成和表现力

- “表现力”是成员详情上方的四个输入值：三项属性与体力。
- 右侧带颜色底的百分比不是表现力输入。
- 支援加成从成员详情右上角信息入口读取，每一方队伍只读取一次并用于该方所有成员模拟。

## 读取节拍

技能卡身份和强化徽标首先按两排批量识别；只有确认存在自定义强化并需要确定具体 ID，或卡面身份／零强化状态仍需消歧的槽位才打开详情。若卡图近邻身份冲突，只有详情事务、两张新鲜详情帧、精确标题、完整强化语义和关闭后同一卡位内容全部一致时，详情标题才可安全完成消歧；成功消歧单独记为信息，不算错误或假设兜底。P 道具先走固定图库缩圈和精排，只有歧义槽才打开详情。这样把交互次数限制在有信息收益的槽位。

零强化是显式状态，不会因目录允许强化而被丢弃。只有卡面费用恰好留下“未强化／费用强化”两个合法状态、详情也无法继续裁决时，才使用降低己方胜率的方向性假设：己方取未强化，对手取已强化；流程继续，但日志会逐卡记录队伍、舞台、成员、排和槽位。身份、强化总次数、效果语义或页面状态的其他歧义仍然失败关闭。

完整读取三个对手可能需要数分钟，且随自定义强化数量变化。

模拟超时默认 **300 秒（5 分钟）**，可设置为任意正整数。它是一次模拟计算的最长时间，算完立即继续，不会固定等待 300 秒，也不包含读取游戏画面的时间。增加模拟次数或设备较慢时可调高；超过上限会停止本次任务。

首次运行会自动选择适合设备的并行数量，结果保存在本机供后续复用，不需要人工试跑或设置核心数。

## 挑战后的安全状态

挑战开始前尚未恢复的失败不会主动点击开始。详情关闭、支援弹层、短暂通信、结果读取和保存等已支持的故障由客户端自动有界恢复。结果已经保存但尚未回场时继续完成回场；有未完成对局时先承接该对局，不重新发送挑战开始。

恢复额度耗尽仍不能确认结果时，程序保留待决记录并停止下一场，避免重复消耗机会。不要手工删除待决文件；按[故障排查](TROUBLESHOOTING.md)收集脱敏错误信息并反馈。

本版作为非预发布 Latest 提供。竞技场已完成多轮管理员 Maa 实机闭环；本次转场结果恢复的初步修复仅完成离线回归，尚未新增实机验收。物理 540×960 的完整日常流程、同一版本连续两日每天 5 场，以及其余跨窗口和独立样本验收仍未全部完成。
